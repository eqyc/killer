apiVersion: v1
kind: Service
metadata:
  name: maintenance-service
  namespace: killer-erp
spec:
  selector:
    app: maintenance-service
  ports:
    - port: 80
      targetPort: 8080
      name: http
