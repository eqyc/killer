apiVersion: v1
kind: Service
metadata:
  name: controlling-service
  namespace: killer-erp
spec:
  selector:
    app: controlling-service
  ports:
    - port: 80
      targetPort: 8080
      name: http
