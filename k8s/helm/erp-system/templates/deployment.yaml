apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "erp-system.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "erp-system.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "erp-system.name" . }}
    spec:
      containers:
        - name: api-gateway
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8080
